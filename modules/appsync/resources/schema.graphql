schema {
  query: Query
  mutation: Mutation
}

type Query {
  singleUser(id: ID!): User
}

type Mutation {
  createUser(name: String!): User
}

type User {
  id: ID!
  name: String!
}
